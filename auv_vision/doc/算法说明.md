# 算法说明 — AUV 视觉项目（RDK X5）构建思路（v3）

> **当前状态（20260917）**：过门（gate）= **扁平 v1.2 版**（`gate/*.py`，8 个文件；来自
> `bak/gate_before_enhance_20260915_182038/` 的 v1.2 原版），**当前定版**。
> `kpt_memory`（门角点逐点软融合）为**可选功能、默认开启**（= 周一 09-14 原行为）：
> 关掉用 `cfg/vision.yaml → vision.gate.kpt_mem.enable: false` 或 `AUV_GATE_KPT_MEM=0`
> （优先级 `AUV_GATE_KPT_MEM` > `enable` > 兜底 `false`）。
> 小球 = `task1_2/ball.py`（已定）；**返回出发区（记忆返回）已整体移除**。
> v1.3/v1.4 分层 gate、质量分 Q、线索 cues、帧守卫（`common/frame_stamp.py`）、
> `common/streak.py`、DOF ramp（`common/ramp.py`、`comm.ramp.dof_per_s`）、`comm.handover`、
> `comm.back`、`STATE_BACK` 均为**已确定不需要、已删除**的功能，下文不再描述。

> 总览与代码阅读入口；**gate 新架构设计与移植来源见 `算法说明-gate-PnP移植方案.md`**；用法见 `README.md`。

## 1. 平台与总体决策

| 项 | 决策 |
|---|---|
| 平台 | RDK X5（Ubuntu 22.04 arm64；`.bin` / march `bayes-e`） |
| 推理 | mock → hbm_runtime/pyeasy_dnn/onnx；**ball 与 gate 权重分离** |
| 相机 | 前视 USB 1280×720（已标定 RMS 0.705）；下视 IMX415 **保留**（素材/未来任务；策略不用） |
| 任务 | 一 撞球（前视，task1_2/ball.py）→ 三 过门（gate/） |
| 编排 | 任务一由 `task1_2/run_ball_reverse.sh` 编排（撞球后定时直线倒车）；`main.py` 状态机负责单任务（ball/gate） |
| 图像链路 | `common/preprocess.py`：去畸变→WB/CLAHE/gamma→640（共用，勿改） |

## 2. 目录分区与职责

```
auv_vision/
├── main.py            # 装配/状态机（唯一装配点；ball/gate）
├── preview_detect.py  # 下水前视觉自检（带识别框；--gate-kpt 看门框 + 4 角点）
├── manual/            # 手动/素材：recorder.py(录像) · stream.py(推流/接收) · udp_server.py(遥控桥)
├── base/              # 基础部件
│   ├── settings.py    #   YAML→S.vision.*/S.comm.*（默认 cfg 在工程根）
│   ├── camera.py      #   SimCamera/UsbCamera/MipiCamera + create_camera(front|down)
│   ├── uart.py        #   11B 帧、DOF→轴映射、心跳、急停、**限深保护(上浮不得越水面)**
│   └── telemetry.py   #   下位机遥测上行 14B(0xAA55+深度/姿态+校验和) 解析/造帧
├── common/            # 跨任务公用（不掺任务逻辑）
│   ├── PID.py         #   PID（ball/gate 共用）
│   ├── cfgnode.py     #   cfg 节点读取 + **ball/gate 共用运动参数的唯一入口**
│   ├── turn_deg.py    #   按角度原地转（遥测 yaw 闭环；gate 正航向与脚本共用）
│   ├── preprocess.py  #   识别前图像链路
│   └── detector.py    #   DetectorHub（Det 可选 kpts；按任务注册）
├── task1_2/           # 任务一（撞球）
│   ├── ball.py        #   任务一 BallTask（撞球）
│   └── run_ball_reverse.sh  #   编排（待机→下潜→前进→撞球→定时直线倒车）
├── gate/              # 任务三 过门（**扁平 v1.2**：keypoint 四角 + PnP；细则见 gate-PnP 文档 §5）
│   ├── gate_task.py（相位机骨架）· mock.py（仿真后端）· __init__.py（门面）
│   ├── gate_detector.py（board_camera/build_gate_backend）· gate_decode.py（keypoint 解码）
│   ├── gate_frontend.py（keypoints_to_img_pts/mode）· geometry.py（CameraModel/PnP/反投影）
│   ├── heading_align.py #  ALIGN.HDG 离散正航向（测→转→停稳→再测）
│   └── kpt_memory.py  #   角点逐点软融合（**可选，默认关闭**；开起来用 enable=true 或 AUV_GATE_KPT_MEM=1）
├── bak/               # 归档（gitignored，不上板）：gate v1.2 原版树 + 两份 v1.2 cfg 快照
├── cfg/               # vision.yaml（纯视觉）· comm.yaml（通信/运动/时序）· front_camera.yaml（标定）
├── doc/  tools/  models/  tests/
```

依赖规则：任务代码只 import `base`/`common` 与同级模块；`main.py` 为装配层
（注册 gate 后端、按 `comm.tasks.enabled` 组任务）。

## 3. 参数组织与图像链路

- `cfg/vision.yaml`：camera(front/down)、`image.*`、`model.*`（mode/path/labels/`task_models.gate`）、
  `gate.*`（几何/keypoint/PnP/kpt_mem 开关）、sim、debug；
- `cfg/comm.yaml`：serial/dof_map/心跳/急停/ramp/`depth_guard`/motion + `tasks.enabled` + `ball/gate`；
- 代码读 `S.vision.*`/`S.comm.*`；`AUV_CFG_DIR`/`AUV_VISION_CFG`/`AUV_COMM_CFG` 可切配置；
- 训练/推理同域（ball 已验证）：训练图经同一 `undistort+enhance+640`；`common/preprocess.py` 不改。

## 4. 状态机与编排

```
IDLE ──► [BALL ──► GATE] ──► DONE          （main.py，按 tasks.enabled）
撞球真机流程（task1_2/run_ball_reverse.sh）：
  待机 → (可选)下潜 → (可选)前进 → main --task ball → 定时直线倒车返回
```
- `main.py --task ball|gate|all`；`all` 按 `comm.tasks.enabled`（默认 `[ball]`）执行；
- gate 需后端：`model.mode=mock` → MockGateBackend；否则 `model.task_models.gate.path`（.bin）。

## 5. 任务算法要点

### 5.1 撞球（task1_2/ball.py）
运动链（视觉识别与运动逻辑都在本项目内，ball 权重为 bbox 检测）：

| 相位 | 下发 | 转移 |
|---|---|---|
| SEARCH | 脉冲旋转（spin/pause）+ 周期性慢速前进探测 | 见球 → CENTER |
| CENTER | **仅 yaw + heave**（surge=0、sway=0），水平+竖直居中 | 连续 `center_confirm_frames` 帧居中 → APPROACH |
| APPROACH | **分级前进 + 仅 sway**（yaw=0、heave=0） | 面积(EMA) ≥ `dash_ratio` 连续 `dash_confirm_frames` 帧 → DASH |
| DASH | `surge_fast`（分级最高速）恒速 `dash_dur_s`，**不检测是否撞到** | 计时到 → STOP |
| STOP | 全 0 保持 `stop_hold_s`（稳定停住） | → DONE(hit) |

丢目标沿用原阶梯：短时保持/惯性 → hold（原地全 0，`engage_hold_s`）→ 回 SEARCH；
**CENTER 段丢失绝不前进**。总时限 `comm.ball.timeout_ms`（DASH/STOP 期间不打断）。

### 5.2 过门（gate/，**扁平 v1.2**；完整设计见 gate-PnP 文档）
- keypoint（门 + 外轮廓四角 TL/TR/BR/BL）→ `solvePnP(IPPE)`+RefineLM → 6-DoF，深度 `Z=tvec.z`；
- 门 = 闭合矩形框（红 PVC，0.70×0.50 m，悬空），前后对称无朝向要求；
- 相位机 SEARCH→RANGE_ALIGN(GOLDEN/**HDG**/CREEP/HOLD/REACQUIRE)→APPROACH→THROUGH；
  HDG=离散正航向（测→转→停稳→再测，2026-09-18 新增，见 gate/heading_align.py）。
  降级链 full/p3p/width/coarse。
- **SEARCH = 左右平移扫视**（`gate.search.{sweep_s,pause_s,sway}`，波形对称来回）：
  **2026-09-20 用户定：过门过程中不允许旋转搜索**，原「旋转脉冲」已删除。
- **水平居中只有 sway 平移**（全档位一套像素门槛 `align.px_x/px_y`）：
  **2026-09-20 用户定：删除 `align_yaw.{enable,in_px,in_pose}` 及其 PID**。
  gate 里唯一的 yaw 来源是 ALIGN.HDG 正航向（增益取 `comm.motion.turn_pid`）。
- **直冲出口（任一成立即 THROUGH：只前进、不做横向微调）**：
  ① 位姿可信且 `Z≤z.cross` 连续 `z.cross_confirm_frames` 帧（**实船已验证，勿动**）；
  ② **近距丢门判过门**（ALIGN **与** APPROACH 都生效）：`Z≤z.near_lost_m`
     （**要求 z 新鲜**，见 `z.z_stale_ms`）**或** 末次框占比 ≥ `z.near_lost_ratio`（OR）；
  ③ **在门口超时兜底**（`loiter.*`）：框占比 ≥ `near_lost_ratio` 且 `|dx|≤dx_max`、`|dy|≤dy_max`
     连续超过 `loiter.timeout_ms` → 自己拍板直冲。
  ⚠️ 旧文档里的 ③④「width/coarse 对准就直冲」**已整体删除**（2026-09-18 用户定）。
- **过门计数**：THROUGH 内按 `through.confirm_ms` **时长**计数（2026-09-18 由帧数改为时长：
  帧数语义会让冲刺时长随 fps 漂，低帧率时"冲不出去"，现场实测 900→2500ms）→ `pass+1`；
  `confirm_ms<=0` 才退回帧数语义（`confirm_frames`）。达 `pass_target` → `DONE(pass)`。
- 角点逐点融合 `gate/kpt_memory.py`：**可选功能、2026-09-18 用户定默认关闭**（`enable: false`；
  板端曾因 `enable: flase` 拼写错被 `bool('flase')=True` 而实际一直开着）。
  开启用 `enable: true` 或 `AUV_GATE_KPT_MEM=1`；关闭时角点单帧直用。
- 配置健壮性：`gate_task` 所有 cfg 读取走 `common/cfgnode.py` 的 `sub/merge/num` 兜底，**缺键时用「等于当前 cfg 的默认值」**，
  `AUV_CFG_DIR` 指向缺新键的旧配置不会 AttributeError。

## 6. 串口协议（下发 11B / 上行 14B，与下位机对齐）

```
下发: 0xA5 | axis0..axis6 | btn0..btn2
axis0=yaw axis1=surge axis2=heave axis3=sway（中位128，极性>128 正向）
任务 → DOF → comm.yaml dof_map → 轴字节（ramp 平滑）；心跳≥20Hz；急停=中位锁定

上行: 0xAA 0x55 | depth_cm | target_cm | roll_cd | pitch_cd | yaw_cd | checksum
      (int16 小端 ×5，从 byte3 起；checksum = sum(前13B) & 0xFF；见 base/telemetry.py)
```

**限深保护（base 层唯一一处，任务不感知）**：每次发帧前读空接收缓冲 → 最新深度；
`comm.depth_guard` 判定 `depth ≤ min_depth_m`(默认 0.3m) 时本帧**禁止上浮**
（heave 清零 + heave 轴当帧回中 `snap`），surge/sway/yaw 原样——保护不干扰对准/前进；
遥测缺失或超时(`stale_ms`)按 `stale_action` 处理：`pass`(默认)放行 / `block_up` 连盲上浮也不许
（SIM 无串口始终放行）。手动模式(udp_server 键盘遥控)同样受此限制。

## 7. 仿真与测试（无硬件）

无硬件用例统一放在 `tests/`（**80 例 / 7 个文件**，**按层分子目录**，统一用 pytest），覆盖：

| 层 / 文件 | 覆盖 |
|---|---|
| `platform/test_base.py` | settings 真实取值 + **限深下限 0.55 不变量**；11B 下发帧布局/极性；DOF→轴字节；**真串口(pty)** 收发；0xAA55 14B 遥测解析与校验错重同步；深度不足禁上浮、下潜与横向照旧、超时/关开关放行；`stop_hard`/急停真的停住 |
| `platform/test_common.py` | PID（死区/限幅/微分/抗 windup）；图像链路（NV12 打包色度保真、**squish 而非 letterbox**）；Det 助手与按类挑选；**cfgnode**（缺键兜底、字符串布尔坑、共用 `comm.motion` 参数唯一来源） |
| `tasks/test_ball.py` | 撞球相位机：SEARCH→CENTER→APPROACH→DASH→STOP→DONE(hit)；CENTER 只 yaw+heave 不前进；APPROACH 只 sway；无目标跑满时限 |
| `tasks/test_gate_vision.py` | PnP 合成往返（含反投影闭环）；keypoint 解码约定（网格项 `+索引` 的 −0.5 陷阱、通道布局、DFL/框心、squish 回投、多尺度 NMS）；mode 降级表；kpt_mem 开关优先级与平滑；mock 后端整链路 |
| `tasks/test_gate_flow.py` | 相位机仲裁：coarse 只有未对准才后退；水平只有 sway、yaw 只在 ALIGN.HDG；SEARCH 是平移扫视；近距丢门判过门、loiter 门口兜底、THROUGH 按时长；陈旧 z 不误判；正航向接入与"不被画面干扰"；**配置守卫**（兜底==cfg、共用值只在 motion） |
| `tasks/test_motion.py` | 额定转角 `TurnCore`（遥测闭环/超时/无遥测拒转/盲转角速率/探向）；离散正航向 `HeadingAligner`（测→转→停稳→再测、迭代上限、单次不设限、p3p 不进滤波器、新鲜度、丢门中止、一键关闭） |
| `tooling/test_pnp_calib.py` | `tools/analyze/pnp_calib.py` 的合成往返：文件名真值解析；标尺反演（**故意把 cfg 门宽写大 20%** → 必须报 a≈1.2 并建议新 `frame_w`）；t_x/t_y 的尺度与符号（t_y 与 up 反号）；psi≈−yaw；p3p 分类与深度偏差；汇总/逐帧 CSV 与报告行数 |

运行方式：`python3 -m pytest tests/ -q`；只跑某一层 `python3 -m pytest tests/platform -q`（或 `tests/tasks` / `tests/tooling`）；
板的部署/全量自检见 `tools/README.md`，测试分层与"会咬"自检的说明见 `tests/README.md`。
"会咬"自检（改 cfg 就该变红）探针在 `tests/_bite_probe.py`，**不被默认收集**：
`python3 -m pytest tests/_bite_probe.py -q`。


## 8. 部署与模型

1. 训练：ball 沿用；gate 用 keypoint 数据集（外轮廓四角，允许只标可见角）；
2. 转换：`.pt → ONNX → OE makertbin(.bin)`，填 `model.task_models.gate.path`；
   **.bin 导出后先做 keypoint 解码自检（设计文档 §6）**；
3. 打包：arm64 deb → 镜像 `/app/auv_vision`。

## 9. 阅读顺序

`README.md` → 本文档 → `算法说明-gate-PnP移植方案.md`（gate 设计与移植来源）→ `算法说明-gate-角点逐点融合滤波.md`（kpt_memory）→ `实验待测-runbook.md`（**PnP/深度标定实验**）。

**待研究项**（未实现、只在纸面）：`待研究-缺角位姿先验（三维信息复用）.md` —— 缺角时用存下来的高质量三维信息补足角点、继续做位姿估算。
