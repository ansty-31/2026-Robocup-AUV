# -*- coding: utf-8 -*-
"""gate 子包 — 任务三「过门」（ball 不可见；设计文档见 doc/算法说明-gate-PnP移植方案.md）

分三层，依赖单向（下层不知道上层），顶层只放装配/仿真：

    gate_task.py   相位机骨架：装配 + 帧守卫 + 相位选路 + 对准档（唯一对外入口 GateTask）
    mock.py        仿真后端：脚本化进近/穿门轨迹（无硬件闭环）
    vision/        视觉处理：像素/模型输出 → 几何量
    data/          数据处理：量 → 干净且带质量标签的数据
    motion/        运动决策：数据 → DOF

细则、参数配置指南、实验待测项见 `gate/README.md`。
"""
